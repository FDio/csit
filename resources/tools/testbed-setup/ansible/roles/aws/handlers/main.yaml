---
# file: roles/aws/handlers/main.yaml

- name: Reboot server
  reboot:
    reboot_timeout: 3600
  tags:
    - reboot-server

- name: Update APT cache
  apt:
    update_cache: "yes"
  tags:
    - update-apt-cache
