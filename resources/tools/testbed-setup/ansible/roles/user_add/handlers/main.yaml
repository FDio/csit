---
# file: roles/user_add/handlers/main.yaml

- name: Restart sshd
  become: true
  become_method: "sudo"
  become_user: "root"
  service:
    name: sshd
    state: restarted
  tags:
    - restart-sshd

