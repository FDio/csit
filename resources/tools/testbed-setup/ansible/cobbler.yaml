---
# file: cobbler.yaml

- hosts: cobbler
  remote_user: testuser
  become: yes
  become_user: root
  gather_facts: false
  pre_tasks:
    - name: Gathering Facts
      gather_facts:
      tags:
        - always
  roles:
    - role: cobbler
      tags: cobbler
    - role: docker
      tags: docker
